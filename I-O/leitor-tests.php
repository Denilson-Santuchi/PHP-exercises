<?php

echo file_get_contents('lista-tests.txt');

// lendo linha a linha
/* $archive = fopen('lista-tests.txt', 'r');

while (!feof($archive)) {
    $line = fgets($archive);

    echo $line;
}

fclose($archive); */
